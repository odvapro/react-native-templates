import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	time: {
		marginBottom: 9,
		textAlign: 'center',
		color: '#000',
		fontSize: 14,
		lineHeight: 18,
	},
});
export default styles;
